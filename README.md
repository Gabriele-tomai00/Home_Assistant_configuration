# Home Assistant configuration
My personal Home Assistant configuration (used also for backup).

I chose to adopt packages, so each package contains everything related to a specific component of the house, for now:  

`car_corsa.yaml` 
`energy.yaml` 
`silla_prism.yaml`

